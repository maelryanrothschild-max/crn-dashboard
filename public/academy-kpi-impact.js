(function(){
const CSS=`<style>.ki-shell{position:fixed;inset:0;z-index:100126;background:#FBF8F3;color:#2B2B2B;overflow:auto}.ki-top{position:sticky;top:0;z-index:4;background:#1B2436;color:#fff;padding:15px 24px;display:flex;justify-content:space-between;align-items:center}.ki-top button,.ki-btn{background:#fff;border:1px solid #A9812E;color:#1B2436;padding:8px 11px;cursor:pointer;font-weight:800}.ki-wrap{max-width:1180px;margin:auto;padding:28px 20px 60px}.ki-hero,.ki-panel,.ki-card{background:#fff;border:1px solid #D9CFB8}.ki-hero{padding:26px;margin-bottom:14px}.ki-k{color:#A9812E;font-size:11px;font-weight:900;letter-spacing:2px}.ki-hero h1,.ki-panel h2{font-family:Georgia,serif;color:#1B2436}.ki-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px}.ki-card{padding:18px}.ki-card h3{font-family:Georgia,serif;color:#1B2436;margin:5px 0}.ki-delta{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:12px 0}.ki-m{background:#FBF8F3;border:1px solid #EEE9DD;padding:10px}.ki-m b{display:block;font-size:18px;color:#1B2436}.ki-m span{font-size:10px;color:#6B6B6B}.ki-pos{color:#3E7A52!important}.ki-neg{color:#A33E3E!important}.ki-panel{padding:20px;margin-top:14px}.ki-form{display:grid;grid-template-columns:2fr repeat(4,1fr);gap:8px;align-items:end}.ki-form label{font-size:10px;font-weight:900;letter-spacing:1px;display:block;margin-bottom:4px}.ki-form select,.ki-form input{width:100%;padding:9px;border:1px solid #D9CFB8;background:#FBF8F3}.ki-note{margin-top:14px;background:#1B2436;color:#fff;padding:18px;border-left:5px solid #A9812E;font-size:13px;line-height:1.5}.ki-empty{padding:22px;background:#fff;border:1px solid #D9CFB8}.ki-error{padding:14px;background:#FBEAEA;color:#A33E3E}@media(max-width:760px){.ki-form{grid-template-columns:1fr}.ki-delta{grid-template-columns:1fr}.ki-wrap{padding:18px 12px 40px}}</style>`;
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
async function api(url,opt){const r=await fetch(url,{credentials:'same-origin',headers:{'Content-Type':'application/json'},...opt});const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'Ошибка');return j}
function fmt(v,suffix=''){return v===null||v===undefined?'—':`${v}${suffix}`}
function d(v){if(v===null||v===undefined)return'<b>—</b>';return`<b class="${v>0?'ki-pos':v<0?'ki-neg':''}">${v>0?'+':''}${v}%</b>`}
async function open(){let x=document.getElementById('academy-kpi-impact');if(x)x.remove();x=document.createElement('div');x.id='academy-kpi-impact';x.className='ki-shell';document.body.appendChild(x);x.innerHTML=CSS+`<div class="ki-top"><b>CRN ACADEMY · KPI IMPACT</b><button id="ki-close">Закрыть ×</button></div><div class="ki-wrap"><div class="ki-hero"><div class="ki-k">LEARNING → BUSINESS RESULT</div><h1>KPI IMPACT</h1><p>Сравниваем фактические показатели до и после развития навыка. Без автоматических причинно-следственных выводов: панель показывает изменение KPI, а не доказывает, что рост вызван только обучением.</p></div><div id="ki-body" class="ki-empty">Загрузка…</div></div>`;x.querySelector('#ki-close').onclick=()=>x.remove();await render()}
async function render(){const root=document.getElementById('ki-body');if(!root)return;try{const [k,t]=await Promise.all([api('/api/academy-kpi'),api('/api/academy-team')]);const all=t.rows||[],rows=k.rows||[];root.className='';root.innerHTML=`<div class="ki-panel"><div class="ki-k">ADD KPI SNAPSHOT</div><h2>Добавить фактический замер</h2><div class="ki-form"><div><label>СОТРУДНИК</label><select id="ki-e">${all.map(r=>`<option value="${esc(r.employee.id)}">${esc(r.employee.firstname)} ${esc(r.employee.surname)} · ${esc(r.employee.store||'')}</option>`).join('')}</select></div><div><label>ДАТА</label><input id="ki-date" type="date" value="${new Date().toISOString().slice(0,10)}"></div><div><label>UPT</label><input id="ki-upt" type="number" step="0.01"></div><div><label>КОНВЕРСИЯ %</label><input id="ki-conv" type="number" step="0.1"></div><div><label>СРЕДНИЙ ЧЕК</label><input id="ki-check" type="number" step="1"></div></div><div style="margin-top:10px"><button class="ki-btn" id="ki-save">СОХРАНИТЬ ЗАМЕР</button></div><div id="ki-msg"></div></div><div class="ki-grid">${rows.length?rows.map(r=>card(r)).join(''):'<div class="ki-empty">Пока нет двух или более KPI-замеров на сотрудника. Добавь baseline и повторный замер — после этого появится динамика.</div>'}</div><div class="ki-note"><b>МЕТОДИКА:</b> фиксируем baseline до обучения, затем повторяем замер через согласованный период — например 14 или 30 дней. Смотрим направление изменения UPT, конверсии и среднего чека вместе с наблюдением руководителя и другими факторами магазина.</div>`;root.querySelector('#ki-save').onclick=save}catch(e){root.innerHTML=`<div class="ki-error">${esc(e.message)}</div>`}}
function card(r){const e=r.employee||{},p=r.pair;if(!p)return`<div class="ki-card"><div class="ki-k">${esc(e.store||'')}</div><h3>${esc(e.firstname)} ${esc(e.surname)}</h3><div>Замеров: ${r.items.length}. Нужен минимум второй замер для сравнения.</div></div>`;return`<div class="ki-card"><div class="ki-k">${esc(e.store||'')}</div><h3>${esc(e.firstname)} ${esc(e.surname)}</h3><div style="font-size:12px;color:#6B6B6B">${esc(p.before.date)} → ${esc(p.after.date)}</div><div class="ki-delta"><div class="ki-m"><span>UPT</span>${d(p.delta.upt)}<small>${fmt(p.before.upt)} → ${fmt(p.after.upt)}</small></div><div class="ki-m"><span>КОНВЕРСИЯ</span>${d(p.delta.conversion)}<small>${fmt(p.before.conversion,'%')} → ${fmt(p.after.conversion,'%')}</small></div><div class="ki-m"><span>СРЕДНИЙ ЧЕК</span>${d(p.delta.avgCheck)}<small>${fmt(p.before.avgCheck)} → ${fmt(p.after.avgCheck)}</small></div></div></div>`}
async function save(){const b=document.getElementById('ki-save');b.disabled=true;try{await api('/api/academy-kpi',{method:'POST',body:JSON.stringify({employeeId:document.getElementById('ki-e').value,date:document.getElementById('ki-date').value,upt:document.getElementById('ki-upt').value,conversion:document.getElementById('ki-conv').value,avgCheck:document.getElementById('ki-check').value})});await render()}catch(e){document.getElementById('ki-msg').innerHTML=`<div class="ki-error">${esc(e.message)}</div>`;b.disabled=false}}
window.CRNAcademyKPI={open};
})();
